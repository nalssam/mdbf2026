# mdbf2026 — 교실 학습 콘텐츠 모음

이 저장소에는 두 개의 학습 프로젝트가 함께 들어 있습니다.

| 프로젝트 | 바로 가기 | 설명 |
|---|---|---|
| 🗝 **6·25 전쟁 스토리텔링 방탈출** | <https://nalssam.github.io/mdbf2026/korean-war-escape/> | 설치 없이 브라우저에서 바로 플레이 (로그인 불필요) |
| ⛏ **BlockQuest** | <https://nalssam.github.io/mdbf2026/> | 서버 없이 학생용 데모(3D 월드 + 광합성 퀴즈) 체험 |

---

# 🗝 역사의 문을 열어라 — 6·25 전쟁 스토리텔링 방탈출

6·25 전쟁의 **원인과 경과**를 몰입형 스토리텔링 방탈출로 학습하는 웹 게임입니다.
루트의 `index.html` 하나로 동작하며, AI(Higgsfield)로 제작한 삽화·배경음악·효과음·이야기 영상이 포함되어 있습니다.

> 🌐 **공개 주소**: <https://nalssam.github.io/mdbf2026/korean-war-escape/> — 학생들은 로그인 없이 바로 접속합니다.

## 게임 구성

박물관 '6·25 전쟁 특별 전시실'에 갇힌 플레이어가 다섯 개의 방을 통과하며 탈출합니다.
방마다 **삽화 배경 위에서 한 줄씩 눌러 읽는 스토리**가 흐른 뒤 퍼즐이 열립니다.

| 방 | 학습 주제 | 퍼즐 |
|---|---|---|
| 1. 기록 보관실 | 전쟁의 배경·원인 (광복, 38도선 분단, 남북 정부 수립, 애치슨 선언) | 문서 해독 퀴즈 → 금고 `3848` |
| 2. 새벽의 사이렌 | 전쟁 발발 (기습 남침, 서울 함락, 유엔군 참전, 낙동강 방어선) | 사건 카드 시간 순서 맞추기 → `0625` |
| 3. 작전 상황실 | 인천 상륙 작전 (맥아더, 서울 수복, 압록강 북진) | 지도에서 상륙 지점 찾기 → `0915` |
| 4. 얼어붙은 겨울 | 중국군 개입과 후퇴 (흥남 철수, 1·4 후퇴, 전선 교착) | 사건–설명 짝 맞추기 → `0104` |
| 5. 협상 테이블 | 정전 협정과 전쟁의 결과 (휴전선, 이산가족, 휴전 상태) | O/X 퀴즈 5문제 → 탈출 자물쇠 `0727` |

## 몰입·학습 기능

- 🎨 방마다 다른 AI 삽화 배경 (과슈 회화풍 통일), 💬 비주얼 노벨식 타자기 대사 (건너뛰기/다시 보기)
- 🎵 배경음악 + 효과음 (🔊 버튼으로 켜고 끄기)
- 📽 **1분 이야기 영상** — 한국어 내레이션·고딕체 자막, 게임 안에서 바로 재생
- 📚 **6·25전쟁 학습콘텐츠** — 왼쪽 탭에서 유튜브 학습 영상 8편을 썸네일로 고르고 게임 안에서 시청
- 📓 학습 노트 자동 정리, 💡 2단계 힌트, ⏱ 타이머, 탈출 후 전체 타임라인 정리

## 에셋 관리

`assets/sources.json`에 생성 미디어의 원본 URL이 기록되어 있습니다.
**Fetch game assets** 워크플로가 미디어를 내려받아 `assets/`에 커밋하고,
`assets/story.srt` 대본으로 이야기 영상(자막 포함)을 ffmpeg로 조립합니다.

---

# ⛏ BlockQuest — 게임으로 배우는 우리 반 퀴즈

마인크래프트/로블록스 감성의 **게이미피케이션 에듀테크 플랫폼**입니다.
수업이 끝나면 학생들은 태블릿(갤럭시탭·크롬북·아이패드)으로 코드/QR을 통해 접속해
퀴즈 게임을 풀고, 개인 포인트 랭킹과 학급 전체 포인트에 도전합니다.

> 🌐 **라이브 체험판**: <https://nalssam.github.io/mdbf2026/> — 서버 없이 학생용 데모(3D 월드 + 광합성 퀴즈)를 바로 체험할 수 있습니다.
> 멀티플레이·교사(관리자)·AI 퀴즈 생성 등 전체 기능은 아래 [실행 방법](#실행-방법)대로 Node 서버를 실행해야 동작합니다.

## 주요 기능

### 🧊 3D 멀티플레이 월드 (로블록스 스타일)
- **복셀 아레나**: 학급마다 같은 시드로 생성되는 잔디 섬 — 나무, 벽돌 유적, 점프 발판 코스, 아이템 상자
- **캐릭터 조작**: 가상 조이스틱(터치)·WASD 이동, 스페이스/버튼 점프 + **이중 점프**, 드래그 카메라 회전
- **블록 파괴·설치**: 곡괭이로 벽돌/상자를 부수고, 블록을 쌓아 나만의 공간 만들기 (모든 변경이 반 전체에 실시간 동기화)
- **아이템**: 👢스피드 부츠(가속) · 🔫블래스터(원거리 파괴) · 💣폭탄(범위 폭파) · 💎블록 꾸러미 — 상자를 부수면 전리품 드랍
- **멀티플레이**: 반 친구들의 아바타가 실시간으로 보이고 함께 뛰어다닌다 (Socket.IO 위치 동기화, 10Hz)
- **퀴즈 퀘스트**: 선생님이 퀴즈를 시작하면 월드 곳곳에 **황금 ❓블록**이 등장 — 찾아가서 문제를 풀면 포인트 획득, 정답 시 블록이 ✓로, 오답 시 ✗로 변한다

### 🎮 학생
- **간편 접속**: 6자리 학급 코드 입력 또는 QR 스캔 → 닉네임 + 복셀 아바타 선택 후 바로 입장 (설치·회원가입 불필요)
- **퀴즈 게임**: 정답을 맞히면 블록 파괴 이펙트 + 포인트 획득, 빠르게 맞히면 스피드 보너스, 연속 정답 콤보 보너스
- **랭킹**: 개인 포인트 랭킹(실시간)과 학급 전체 포인트 게이지, 레벨/XP 시스템, 포디움 결과 화면
- **클래식 모드**: 3D가 부담스러운 저사양 기기는 `play.html` 클래식 모드로 동일한 퀴즈 참여 가능

### 🧑‍🏫 교사 (관리자)
- **AI 퀴즈 자동 생성**: PPT(pptx)·PDF·Excel(xlsx)·Word(docx)·텍스트 파일 업로드, 기사/유튜브 URL, 또는 복사-붙여넣기
  → AI가 자료를 분석해 **학습목표 + 평가 문항(4지선다/OX) + 해설**을 자동 생성
- **퀴즈 검토·수정**: 생성된 문항을 편집한 뒤 원클릭으로 학생들에게 배포(LIVE)
- **실시간 현황**: 학생 × 문항 풀이 매트릭스가 실시간으로 갱신 (Socket.IO)
- **학급 인사이트 시각화**: 문항별 정답률, 학습목표 도달률, 학생 성취 분포(도넛), 학생별 풀이 히트맵, 자동 분석 코멘트

## 실행 방법

```bash
npm install
npm start          # http://localhost:3000
```

### AI 퀴즈 생성 (Claude API)

```bash
export ANTHROPIC_API_KEY=sk-ant-...   # https://platform.claude.com 에서 발급
npm start
```

- API 키가 있으면 **Claude(claude-opus-4-8)** 가 자료를 해독해 학습목표·문항을 생성합니다 (structured outputs로 JSON 스키마 보장).
- API 키가 없으면 **규칙 기반 폴백 생성기**가 동작해 오프라인/데모 환경에서도 전체 흐름을 체험할 수 있습니다.
- 모델 변경: `ANTHROPIC_MODEL` 환경변수 (기본값 `claude-opus-4-8`)

## 사용 흐름

1. 교사: 첫 화면에서 **새 학급 만들기** → 학급 코드/QR 자동 발급
2. 교사: **AI 퀴즈 탭**에서 수업 자료 업로드 → AI가 학습목표·문항 생성 → 검토 후 **퀴즈 시작**
3. 학생: 태블릿에서 코드/QR로 입장 → 퀴즈가 시작되면 자동으로 게임 화면 전환
4. 교사: **실시간 현황**에서 풀이 상황 모니터링 → 종료 후 **인사이트**에서 학급 분석 확인

## 기술 구성

| 영역 | 내용 |
|---|---|
| 서버 | Node.js + Express 5 + Socket.IO (실시간), JSON 파일 영속화 (`data/db.json`) |
| 3D 월드 | three.js(로컬 서빙) + 커스텀 복셀 물리(AABB·이중 점프·DDA 레이캐스트) + InstancedMesh 렌더링 |
| 월드 동기화 | 학급별 지형 diff 저장(늦게 입장해도 동일 지형), 이동/파괴/설치/폭탄 브로드캐스트, 레이트리밋·지형 보호 검증 |
| 자료 추출 | pdf-parse(PDF), JSZip(PPTX/DOCX), SheetJS(XLSX/CSV), fetch(기사/유튜브 메타데이터) |
| AI | `@anthropic-ai/sdk` — Claude Messages API + JSON schema structured outputs, 키 없을 시 휴리스틱 폴백 |
| 프론트 | 바닐라 JS + 캔버스 픽셀 텍스처/복셀 아바타/파티클/8비트 사운드(WebAudio), 반응형(태블릿 최적화) |
| 시각화 | 경량 SVG 차트 (접근성 검증 팔레트, 색+기호 이중 표기) |

## 배포

| 방식 | 주소 | 제공 범위 |
|---|---|---|
| **정식 서비스 (Render)** | 아래 버튼으로 배포 | ✅ 전체 기능 — 교사 관리자·AI 퀴즈 생성·실시간 멀티플레이·인사이트 |
| 체험판 (GitHub Pages) | <https://nalssam.github.io/mdbf2026/> | 학생용 데모 (3D 월드 + 데모 퀴즈, 서버 없음) |
| 체험판 미러 (Vercel) | main 푸시 시 자동 배포 | 학생용 데모 (위와 동일) |

### 🚀 정식 서비스 원클릭 배포 (Render, 무료)

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/nalssam/mdbf2026)

1. 위 버튼 클릭 → [Render](https://render.com) 로그인(구글 계정 가능) → **Deploy** 클릭
2. 배포 화면에서 `ANTHROPIC_API_KEY` 입력 (선택 — [platform.claude.com](https://platform.claude.com)에서 발급. 비워 두면 규칙 기반 폴백 생성기로 동작)
3. 몇 분 뒤 발급되는 `https://blockquest-****.onrender.com` 주소가 정식 서비스 주소입니다 — 교사·학생 모두 이 주소로 접속

무료 플랜 참고사항:
- 15분간 접속이 없으면 잠들었다가 다음 접속 때 깨어납니다(약 1분 소요). **수업 시작 5분 전에 교사가 먼저 접속**해 깨워 두면 학생들은 바로 들어올 수 있어요.
- 서버가 재시작되면 학급·포인트 데이터가 초기화됩니다(무료 플랜은 디스크가 임시 저장소). 수업 차시 단위로 학급을 새로 만들어 쓰는 흐름에는 문제가 없고, 데이터를 계속 보존하려면 Render 유료 디스크를 붙이면 됩니다.

### 다른 곳에 배포 (Docker)

`Dockerfile`이 포함되어 있어 Railway·Fly.io·학교 서버 등 어디서든 동일하게 실행할 수 있습니다:

```bash
docker build -t blockquest .
docker run -p 3000:3000 -e ANTHROPIC_API_KEY=sk-ant-... blockquest
```

### 배포 메모

- 데이터는 `data/db.json`에 저장됩니다 (`DATA_DIR` 환경변수로 위치 변경 가능).
- 포트는 `PORT` 환경변수로 변경할 수 있습니다.
- 헬스체크 엔드포인트: `GET /healthz`
- 학생 QR은 `서버주소/?code=학급코드` 로 연결되므로, 배포 시 학생 기기에서 접근 가능한 주소로 서비스해야 합니다.
